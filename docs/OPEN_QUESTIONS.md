# Open Questions

Confirmed decisions have been moved into `SCORING_RULES.md`, `MATCHMAKING_RULES.md`, `DATA_MODEL.md`, `ROADMAP.md`, and `IMPLEMENTATION_TICKETS.md`.

## Remaining Non-Blocking Questions

1. What is the final season end date once the August schedule is set? This should remain editable rather than blocking implementation.

## Blocking Ambiguity

- None for database schema work. Keep `seasons.ends_on` nullable/editable until the final date is known.
